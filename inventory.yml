---
all:
  children:
    cloudvision:
      hosts:
        cvaas:
          # Ansible variables for CVaaS connection
          ansible_httpapi_use_ssl: true
          ansible_httpapi_validate_certs: true
          ansible_httpapi_port: 443
          ansible_network_os: eos
          ansible_connection: httpapi
          ansible_user: avdsrvaccnt
          ansible_password: "{{ lookup('env', 'CVP_PASSWORD') }}"
          ansible_host: www.cv-prod-us-central1-c.arista.io
    CAMPUS:
      vars:
        ansible_user: admin
        ansible_ssh_pass: admin
        ansible_network_os: eos
        ansible_connection: network_cli
        ansible_become: yes
        ansible_become_method: enable
        ansible_become_password: admin
      children:
        BD1:
          children:
            SPINES:
              hosts:
                SC-B1-Core1:
                  ansible_host: 10.10.0.21
                SC-B1-Core2:
                  ansible_host: 10.10.0.22
            LEAVES:
              hosts:
                SC-B1-IDF1:
                  ansible_host: 10.10.0.10
    NETWORK_SERVICES:
      children:
        SPINES:
        LEAVES:


    